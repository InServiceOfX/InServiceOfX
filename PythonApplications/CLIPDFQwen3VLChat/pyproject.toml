[tool.poetry]
name = "clipdfqwen3vlchat"
version = "0.1.0"
description = "CLI: rasterize PDFs and ask Qwen3-VL a user-supplied question per page"
authors = ["Ernest Yeung <ernestyalumni@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

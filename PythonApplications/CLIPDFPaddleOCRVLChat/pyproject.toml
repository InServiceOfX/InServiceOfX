[tool.poetry]
name = "clipdfpaddleocrvlchat"
version = "0.1.0"
description = "CLI: rasterize PDFs and send pages to PaddleOCR-VL served by vLLM"
authors = ["Ernest Yeung <ernestyalumni@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
